// Every route is prerendered to static HTML by default. Individual
// routes can still narrow this (e.g. the podcast page stays prerendered
// but hydrates a client-side fetch on top).
export const prerender = true;
